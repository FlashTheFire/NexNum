# Production HTTPS with automatic Let's Encrypt
nexnum.in, www.nexnum.in {
    reverse_proxy nexnum-app:3000
    encode gzip
}

# HTTP to HTTPS redirect
:80 {
    redir https://nexnum.in{uri} permanent
}
